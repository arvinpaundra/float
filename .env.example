# Copy to .env and fill in. Inlined into the UI bundle at build time (bun build --env='SPOTIFY_*').
# Spotify app Client ID (PKCE: public, no secret). Dashboard: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
